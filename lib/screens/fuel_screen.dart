// Fuel & roadside help screen

import 'package:flutter/material.dart';

class FuelScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fuel & Roadside Help'),
      ),
      body: Center(
        child: Text('Fuel & Roadside Help Screen'),
      ),
    );
  }
}